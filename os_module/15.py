# Python os system function allows us to run a command in the Python script, just like if I was running it in my shell. For example:
import os
os.system("ls")
os.system("touch file1")
os.system("ls -l file1")